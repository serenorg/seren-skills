SEREN_API_KEY=
# Optional. Without this, Surface B watchlist still renders read-only;
# only the "Create this market on Prophet" deep links are suppressed.
PROPHET_SESSION_TOKEN=
# Optional — required only for `--yes-live`. The execution-path gate also
# checks `py-clob-client` is installed (see requirements.txt).
POLY_PRIVATE_KEY=
POLY_API_KEY=
POLY_PASSPHRASE=
POLY_SECRET=
