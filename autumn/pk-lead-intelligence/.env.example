# Seren API key. Required for every publisher call this skill makes
# (perplexity, seren-models, google-drive, seren-cron, seren-db).
#
# Three supported sources, in order of preference:
#
#   1. Seren Desktop — leave SEREN_API_KEY empty and the desktop
#      runtime will inject API_KEY for you.
#   2. Claude Cowork / plain Claude Code — install the hosted Seren
#      MCP, which handles web auth and exposes every publisher this
#      skill needs (issue #789):
#        claude mcp add --scope user --transport http seren \
#            https://mcp.serendb.com/mcp
#      Then paste the session key the MCP issues into SEREN_API_KEY
#      below.
#   3. No MCP available — register a fresh agent key:
#        curl -sS -X POST https://api.serendb.com/auth/agent \
#            -H 'Content-Type: application/json' \
#            -d '{"name":"pk-lead-intelligence"}'
#      Copy .data.agent.api_key into SEREN_API_KEY below.
#
# Do NOT register a second account if a key already exists for your
# team — duplicate registrations issue $0-balance keys that override
# your funded one. See SKILL.md > API Key Setup.
SEREN_API_KEY=

# 1Password Service Account token. The skill shells out to the `op`
# CLI to read the Salesforce login item. Generate the SA in 1Password
# admin (one-time, operator-side), grant it read-only access to the
# vault below, then paste the token here. Treat this value as a
# credential — never commit it.
OP_SERVICE_ACCOUNT_TOKEN=

# Name of the 1Password vault holding the Salesforce login item.
# The Service Account above must have read access to this vault.
OP_VAULT=PK Salesforce Skill

# Name of the 1Password item inside the vault. The item must carry
# `username`, `password`, and a TOTP field (the rolling 6-digit MFA
# code). The skill never logs these fields and never persists them
# to disk.
OP_ITEM=PK Salesforce
