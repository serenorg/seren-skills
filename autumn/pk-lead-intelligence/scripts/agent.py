"""Top-level entrypoint for pk-lead-intelligence runs.

Will expose the `run`, `weekly`, and slash-command dispatch paths once
phase 1+ work lands. Empty in phase 0 — exists so the rest of the
codebase can `import scripts.agent`.
"""
