"""Write the rendered enrichment Note to a Lead's Related tab.

Gated on `live_mode=true` in config + `--allow-live` on the CLI. Both
must be present before any Salesforce write fires. Implemented in
phase 4.
"""
